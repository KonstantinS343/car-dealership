[pytest]
DJANGO_SETTINGS_MODULE = src.car_dealership.settings
python_files = test*.py
addopts = --reuse-db --ignore=data
pythonpath = . src

log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
